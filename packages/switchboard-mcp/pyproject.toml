[project]
name = "operator-switchboard-mcp"
version = "0.1.0"
description = "Switchboard MCP tool v0"
requires-python = ">=3.12"
dependencies = ["mcp~=1.28.1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/operator_switchboard_mcp"]
